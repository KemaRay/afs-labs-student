apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: wraymond
spec:
  type: NodePort
  selector:
    app: app-service
  ports:
    - name: "5000"
      port: 5000
      targetPort: 5000
